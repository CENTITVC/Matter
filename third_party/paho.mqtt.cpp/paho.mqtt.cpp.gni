import("//build_overrides/build.gni")
import("//build_overrides/chip.gni")

declare_args() {
  paho_build_static = true  # Default: Build static library
  paho_build_shared = false  # Default: Build shared library (DLL)
  paho_with_ssl = true  # Default: SSL-enabled library (OpenSSL)
  paho_with_mqtt_c = true
}
